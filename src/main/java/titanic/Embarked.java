package titanic;

/**
 * indicates the port of embarkation :
 * - C = Cherbourg (0.25)
 * - Q = QueensTown (0.5)
 * - S = Southampton (1.0)
 */

public enum Embarked {
    NULL, C, Q, S

}